class Test {
    static const var name = "Clarity"

    static fn getName() {
        return Test.name
    }
}

fn main() {

    for var i = 1, i <= 100, i = i + 1 {
        native.print(Test.getName() + " loves you " + i + " times\n")
    }

}