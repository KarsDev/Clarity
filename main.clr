include native system

async -> "async-thread-name (optional)" {
    native.println("async running on " + System.currentThreadName())
}

native.println("sync running on " + System.currentThreadName())
