include native random

str toPrint = ""
for i : 20 {
    var rand = new Random(696969) // always same seed
    toPrint += rand.randInt(0, 10)
    if i != 19 {
        toPrint += ", "
    }
}
native.println(toPrint)