var x
while true {
    try {
        x = int(native.input("Write a number: "))
        break
    } except {
        native.err("Invalid number format.")
        continue
    }
}

native.println("You wrote " + x)