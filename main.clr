include native system

native.println("Starting..." + System.currentThreadName())

async -> "another thread" {
    native.println("Executing on another thread: " + System.currentThreadName())
}

native.println("Continuing...")