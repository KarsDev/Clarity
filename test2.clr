fn add(array) {

    assert array is arr else "Expected parameter of type array"

    var result = 0
    for i : array {
        result = result + i
    }
    return result
}

var i = add(1, 3)

native.println(i)