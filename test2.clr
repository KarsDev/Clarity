fn add(array) {

    assert array is arr else "Expected parameter of type array"

    var result = 0
    for i : params {
        result = result + i
    }
    return result
}

var i = add([1, 2, 3, 4, 5])

native.println(i)