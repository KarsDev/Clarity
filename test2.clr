include native system

fn main() {
    var start = native.nano()
    loop(5 * 10 ^ 6)
    var end = native.nano()
    native.print("took " + (end - start) + "ns")
}

fn loop(max) {
    for var i = 0, i < max, i = i + 1 {
    }
}