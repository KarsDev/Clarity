class Device {
    const var name

    constructor(name) {
        local.name = name
    }

    fn getName() {
        return name
    }

    fn print() {
        return "Device{name=" + local.name + "}"
    }
}

class Phone inherits Device {

}

var phone = new Phone("Samsung")

native.println(phone)