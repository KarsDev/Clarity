include native random

const var rand = new Random(123)

for var i = 0, i < 10, i = i + 1 {
    var start = native.nano()
    rand.randInt(0, 100)
    native.println("Took " + (native.nano() - start) + "ns")
}